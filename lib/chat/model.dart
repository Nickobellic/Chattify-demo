class Task {
  int id;
  String title;
  String description;
  bool done;

  Task(this.id, this.title, this.description, this.done);
}


class Message {
  int message_id;
  String message;

  Message(this.message_id, this.message);
}